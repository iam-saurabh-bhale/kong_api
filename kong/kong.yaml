_format_version: "3.0"

services:
  - name: user-service
    url: http://user-service.default.svc.cluster.local:80
    routes:
      - name: user-route
        paths:
          - /api
        strip_path: false